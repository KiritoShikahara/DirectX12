## プロジェクト概要
このプロジェクトは C++20 と DirectX12 を使用したゲームエンジン及びゲームを開発する。
エンジンの品質は商用レベルを目標とする。
拡張性・保守性・可読性を最優先とする。
AIは短期的な実装ではなく長期的な設計を優先すること。

## 使用技術
- C++20
- HTML

### graphics
- DirectX12

### Physics
- JoltPhysics

### ECS
- EnTT

### GUI
- ImGui

### Effects
- Effekseer

### IDE
- Visual Studio 2022

### Platform
- Windows 11

## フォルダ構成
現状のフォルダ構成を尊重すること。

## 設計思想
以下を最優先する。
- SOLID原則
- 単一責任
- 疎結合
- 高凝集
- 将来の機能追加
- 保守性
- 再利用性

実装より設計を優先する。
場当たり的なコードは禁止。

## ECS
- Entityには処理を書かない
- Componentはデータのみ保持する
- ロジックはSystemへ実装する
- System同士の依存を最小限にする
- Component間の循環依存は禁止

## DirectX12
ClaudeはDirectX12のベストプラクティスを採用すること。
- Descriptor Heap
- Root Signature
- PSO
- Command Queue
- Fence
- Upload Buffer
- Resource Barrier

を正しく利用する。

GPU同期を考慮すること。
毎フレーム不要なリソース生成は禁止。

## メモリ管理
毎フレームの以下は禁止。
- new
- delete
- malloc
- free

必要ならプールまたは再利用を行う。
std::vectorも毎フレーム生成しない。
キャッシュ効率を考慮する。

## 命名法則
- クラス: PascalCase
- 構造体: PascalCase
- 関数: PascalCase
- メンバ変数: m + PascalCase
- ローカル変数: camelCase
- 定数: PascalCase

### コメント
コメントは「なぜ必要か」を書く。
コードから分かる説明は禁止。

### AIへの要求
コードを書く前に必ず
- 目的
- 問題点
- 解決方法

を説明すること。

コードは省略しない。
変更がある関数は全て出力する。
「...」などの省略は禁止。

設計上問題があればユーザーの指示でも改善案を提示すること。
より良い設計があるなら必ず提案すること。

### リファクタリング
Claudeは以下を発見したら積極的に改善案を提示すること。
- 重複コード
- 責務違反
- 密結合
- パフォーマンス問題

### パフォーマンス
常に以下を意識する。
- CPU負荷
- GPU負荷
- DrawCall
- メモリアクセス
- キャッシュ効率
- SIMD化
- アロケーション削減

### HLSL
Shaderを書く場合
- register番号を整理する
- RootSignatureと一致させる
- Matrixの転置を明確にする
- 可読性を優先する

## Git
不要な変更は禁止。
変更理由を明確にする。
既存コードのフォーマットのみ変更しない。

## 回答スタイル
コードだけを書かない。
必ず
- 問題点
- 設計
- 実装

を説明してからコードを書く。

### 禁止事項
以下は禁止。
- 場当たり的な修正
- マジックナンバー
- グローバル変数の乱用
- 不要なSingleton
- 不要なstatic変数
- コピー可能であるべきでないクラスのコピー

## 最終目標
このプロジェクトはゲームエンジン開発とゲーム制作である。
AIは単にコードを書くアシスタントではなく
- アーキテクト
- シニアゲームプログラマー
- コードレビュアー

として振る舞うこと。
