# AGENTS.md

## プロジェクト規約
設計思想・命名規則・禁止事項などの詳細ルールは `CLAUDE.md` を参照すること。
本ファイルはAIエージェント共通のセッション運用ルール（作業の再開・記録方法）を定義する。

## 作業再開時の手順（コンテキスト節約）
新しいセッションでは、リポジトリ全体やgit logを読み込む前に、まず以下の順で読むこと。

1. `claude/STATUS.md` — 直近の状況を数行で把握する
2. `claude/TASKS.md` — 未完了タスクを確認する
3. 必要な場合のみ `claude/DECISIONS.md` を確認する（過去の設計判断が関係する場合）
4. `App/`のゲーム側の作業をする場合は `claude/STRUCTURE.md`（フォルダ構成）と
   `claude/GAME_DESIGN.md`（ゲーム概要・仕様）も確認する

これらのファイルだけで前回までの文脈を復元できることを目標とする。
コード全体の再読み込みや `git log` の全走査は、上記だけで判断できない場合のみ行う。

## セッション終了・区切り時の更新ルール
作業を中断・終了する際は以下を更新すること。

- `claude/STATUS.md`
  - 常に上書き（追記しない）。数行以内に収める。
  - 内容: 現在のブランチ、直近やったこと、次にやるべきこと、未解決の問題
- `claude/TASKS.md`
  - 完了したタスクにチェックを入れる
  - 新たに発生したタスクを追記する
  - 完了して不要になった古いタスクは削除してよい
- `claude/DECISIONS.md`
  - 長期的な設計・アーキテクチャに影響する決定をした場合のみ追記する（追記のみ、削除しない）
  - 日常的な実装の詳細やバグ修正内容は書かない
- `claude/GAME_DESIGN.md`
  - ゲームの仕様・世界観・コアループに変更や新たな決定があった場合のみ更新する
  - 「現状の実装状況まとめ」表はApp側のゲームプレイ機能を実装・変更するたびに更新する

## 記述ルール
- 各ファイルは常に簡潔に保つ。冗長な説明やコード・git履歴から分かる内容は書かない。
- 日付は絶対日付（YYYY-MM-DD）で記載する。
- 憶測や未確認の情報は書かない。
